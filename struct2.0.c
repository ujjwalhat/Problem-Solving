#include<stdio.h>
struct node {
    int data;
    struct node *link;
};
int main()
{





while ( y==2)
{
    if(front==NULL)
    {
        r=(struct node *)malloc(sizeof(struct node));
        r->data= value;
        r->link=NULL;
        front =r;
        rear = r;
    }
    else 
    {
        if(rear->link = NULL)
        {
        r=(struct node *)malloc(sizeof(struct node));
        r->data = value;
        rear->link=r;

        }
    }
    }
}